@model IEnumerable<LVH_Lesson06.Models.LvhSong>

@{
    ViewBag.Title = "Lvh: Danh sách bài hát";
}

<h2>Lvh: Danh sách bài hát</h2>

<p>
    @Html.ActionLink("Create New", "LvhCreate")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LvhTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LvhAuthor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LvhArtist)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LvhYearlease)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LvhTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LvhAuthor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LvhArtist)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LvhYearlease)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
